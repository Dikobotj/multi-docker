sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Dikobotj/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true Dikobotj/react-test npm test

after_success:
  - docker build -t Dikobotj/multi-client ./client
  - docker build -t Dikobotj/multi-nginx ./nginx
  - docker build -t Dikobotj/multi-server ./server
  - docker build -t Dikobotj/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push Dikobotj/multi-client
  - docker push Dikobotj/multi-nginx
  - docker push Dikobotj/multi-server
  - docker push Dikobotj/multi-worker
